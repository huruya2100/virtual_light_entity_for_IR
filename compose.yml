version: '3'
services:
  virtual_light:
    build: .
    network_mode: "host"
  mosquitto:
    image: eclipse-mosquitto:2
    ports:
      - "1883:1883"
    volumes:
      - ./tests/integration/mosquitto.conf:/mosquitto/config/mosquitto.conf:ro
    profiles:
      - test
